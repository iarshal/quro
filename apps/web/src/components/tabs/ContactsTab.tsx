'use client';

export function ContactsTab({ profile }: { profile: any }) {
  return (
    <div className="flex flex-col min-h-full w-full bg-[#EDEDED]">
      <div className="flex-1 flex items-center justify-center text-gray-500">
        <p>Contacts will appear here.</p>
      </div>
    </div>
  );
}
